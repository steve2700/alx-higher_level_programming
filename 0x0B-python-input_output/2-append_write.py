#!/usr/bin/python3
"""This modules defines a file-appending function"""


def append_write(filename="", text=""):
    """Appends a string to the end of the UTB text file"""
    with open(filename, "a", encoding=utf-8") as f
    return f.write(text)
